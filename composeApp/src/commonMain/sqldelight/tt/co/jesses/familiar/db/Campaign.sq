CREATE TABLE Campaign (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    created_at INTEGER NOT NULL
);

selectAll:
SELECT * FROM Campaign;

insert:
INSERT INTO Campaign(name, description, created_at)
VALUES (?, ?, ?);

deleteById:
DELETE FROM Campaign WHERE id = ?;
